/*
 Temporary placeholder main.cpp — the file previously contained the GitHub Actions YAML
 which caused clang++ to try to compile YAML and fail. Replace this with your real mod
 source when you have it available.
*/
int main() {
    return 0;
}
